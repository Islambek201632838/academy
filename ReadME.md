# 🏆 Система классификации товаров

Документ описывает полный рабочий процесс классификации товарных позиций с использованием zero-shot и fine-tuned моделей. Решение подходит для e-commerce систем, маркетплейсов, товарных каталогов и других задач категоризации.

---

## 📁 Структура проекта

```
academy/
├── .venv/
├── .gitignore
├── short.csv
├── classifier.py
├── cross_encoder.py
├── esf_full_20251121949.csv
├── fine-tuning.py
├── requirements.txt
├── sklearn_train.py
├── use_model.py
└── External Libraries/
```

---

## 🚀 Выбор подхода

```
Размеченные данные (DESCRIPTION + Категория)?
        НЕТ                                    ДА (500+)
        │                                        │
        ├─────────────────┬──────────────────────┤
    ┌──▼────┐        ┌────▼─────┐         ┌─────▼────┐
    │Нужна   │        │Достаточно│         │Требуется   │
    │скорость│        │времени?  │         │высокая     │
    └───┬────┘        └────┬────┘         │точность?   │
        │                   │              └─────┬──────┘
    ┌───▼─────────┐   ┌────▼────────┐   ┌───────▼────────┐
    │classifier.py │   │cross_encoder│   │sklearn_train.py │
    └──────────────┘   └─────────────┘   └──────────┬──────┘
                                                     │
                                           Максимальная точность?
                                                     │
                                              ┌──────▼────────┐
                                              │fine-tuning.py  │
                                              └───────────────┘
```

---

# 📖 Руководство

## 1️⃣ Zero-shot решения (без обучения)

---

### `classifier.py` — наиболее быстрое zero-shot решение

**Запуск:**
```bash
python classifier.py
```

**Описание:**
- используется zero-shot классификатор из Hugging Face  
- обучение не требуется  
- корректно работает на CPU  
- **на CPU скорость выполнения ниже в 5–20 раз по сравнению с GPU**  
- подходит для быстрых прототипов и базовой категоризации

**Пример:**
```python
from transformers import pipeline

classifier = pipeline("zero-shot-classification", model="xlm-roberta-large")

categories = ["Обувь", "Одежда", "Продукты", "Услуги"]
text = "Кроссовки мужские Nike"

result = classifier(text, categories)
print(result["labels"][0])
```

---

### `cross_encoder.py` — повышенное качество zero-shot

**Запуск:**
```bash
python cross_encoder.py
```

**Описание:**
- используется CrossEncoder из SentenceTransformers  
- осуществляется ранжирование пар «описание–категория»  
- GPU не обязателен  
- **при большом количестве категорий производительность на CPU существенно снижается**  

**Пример:**
```python
from sentence_transformers import CrossEncoder
import numpy as np

model = CrossEncoder("cross-encoder/ms-marco-MiniLM-L-6-v2")

cats = ["Обувь", "Одежда", "Продукты", "Услуги"]
prod = "Кроссовки мужские Nike"

pairs = [[prod, c] for c in cats]
scores = model.predict(pairs)

print(cats[np.argmax(scores)])
```

---

## ⚠ Особенности выполнения моделей без GPU

### Факт
Zero-shot модели **поддерживают выполнение на CPU**.

### Ограничения
- крупные модели (например, `xlm-roberta-large`) на CPU работают значительно медленнее  
- CrossEncoder с большим числом категорий оказывает высокую нагрузку на CPU  
- обработка больших объёмов данных (50k–200k строк) без GPU требует существенного времени  
- при использовании BERT-подобных моделей GPU рекомендуется

---

## 2️⃣ Классическое обучение (sklearn)

### Обучение  
```bash
python sklearn_train.py
```

Выполняются следующие этапы:
- TF-IDF векторизация  
- обучение Logistic Regression  
- сохранение обученной модели и векторизатора  

### Использование  
```bash
python use_model.py
```

---

## 3️⃣ Fine-tuning (SentenceTransformer, CrossEncoder, BERT)

**Запуск:**
```bash
python fine-tuning.py
```

Поддерживаются варианты:
- SentenceTransformer fine-tuning  
- CrossEncoder fine-tuning  
- BERT sequence classification  

Для обучения рекомендуется наличие GPU.

---

# 📊 Сравнение подходов

| Метод | Требует обучения | Скорость | Назначение |
|-------|------------------|----------|------------|
| classifier.py | Нет | Быстро | Базовое zero-shot |
| cross_encoder.py | Нет | Средне | Улучшенное zero-shot качество |
| sklearn | Да | Очень быстро | Production без нейросетей |
| Fine-tuning ST | Да | Средне | Оптимальный баланс |
| Fine-tuning CE | Да | Средне | Высокое качество |
| Fine-tuning BERT | Да | Медленно | Максимальная точность |

---

# 🎯 Логика выбора

```
Нет размеченных данных → zero-shot (classifier.py / cross_encoder.py)
Есть 500+ примеров → sklearn_train.py
Требуется максимальная точность → fine-tuning.py
```

---

# 🔧 Описание файлов

### `classifier.py`
Zero-shot классификация на Transformers.

### `cross_encoder.py`
Оценка релевантности пар «описание–категория».

### `sklearn_train.py`
Модель TF-IDF + Logistic Regression.

### `use_model.py`
Инференс обученной sklearn модели.

### `fine-tuning.py`
Обучение ST / CrossEncoder / BERT.

---

# 🚀 Пример продакшн-использования

```python
import joblib  
import pandas as pd  

clf = joblib.load("trained_classifier_sklearn.pkl")
vect = joblib.load("trained_vectorizer_sklearn.pkl")

df = pd.read_csv("new_products.csv")

X = vect.transform(df["DESCRIPTION"])
df["category"] = clf.predict(X)
df["confidence"] = clf.predict_proba(X).max(axis=1)

df.to_csv("classified_products.csv", index=False)
```

---

# 🔧 Установка

```bash
pip install -r requirements.txt
```

---

# 💡 Рекомендации

### Подготовка данных
- рекомендуется выполнять очистку текста  
- рекомендуется удалять дубли  
- желательно обеспечивать баланс категорий  

### Модели
- рекомендуется вести журнал метрик  
- рекомендуется сохранять версии моделей  
- необходимо контролировать деградацию качества  

### Продакшен
- рекомендуется фильтрация низкой уверенности  
- мониторинг качества обязателен  

---

# ❓ Часто задаваемые вопросы

**Можно ли использовать модели без GPU?**  
Да, корректное выполнение возможно, но скорость работы значительно ниже, особенно для крупных моделей.

**Какое количество данных подходит для обучения?**  
От 500 примеров; оптимально 1000+.

**Как осуществляется обработка 100k+ товаров?**  
На CPU рекомендуется использование sklearn или SentenceTransformer с батчевой обработкой.  
Zero-shot модели без GPU могут работать значительно медленнее.

---

# 📚 Источники

- https://huggingface.co/models  
- https://www.sbert.net  
- https://scikit-learn.org  
